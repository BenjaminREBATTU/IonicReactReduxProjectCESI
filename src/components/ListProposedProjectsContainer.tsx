import React from "react"
import { IonList } from "@ionic/react"

interface ContainerProps {
    listProposed: any
}

const ListProposedProjects: React.FC<ContainerProps> = ({ listProposed }) => {
    return (
        <IonList>

        </IonList>
    )
}

export default ListProposedProjects
