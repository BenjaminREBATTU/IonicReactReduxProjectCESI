import React from "react"
import { IonList } from "@ionic/react"

interface ContainerProps {
    listInterestedDevs: any
}

const ListInterestedDevsContainer: React.FC<ContainerProps> = ({ listInterestedDevs }) => {
    return (
        <IonList>

        </IonList>
    )
}

export default ListInterestedDevsContainer
